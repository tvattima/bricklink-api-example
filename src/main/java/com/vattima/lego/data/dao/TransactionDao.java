package com.vattima.lego.data.dao;

import com.vattima.lego.data.dto.Transaction;
import com.vattima.lego.data.dto.TransactionItem;
import com.vattima.lego.data.ibatis.mapper.TransactionMapper;
import lombok.RequiredArgsConstructor;
import org.springframework.stereotype.Component;

import java.time.LocalDate;
import java.util.List;

@Component
@RequiredArgsConstructor
public class TransactionDao {
    private final TransactionMapper transactionMapper;

    public void insertTransaction(Transaction transaction) {
        transactionMapper.insertTransaction(transaction);
    }

    public void insertTransactionItem(TransactionItem transactionItem) {
        transactionMapper.insertTransactionItem(transactionItem);
    }

    public List<Transaction> findByTransactionDate(LocalDate date) {
        return transactionMapper.findByTransactionDate(date);
    }

    public List<Transaction> findByAutoGeneratedAndTransactionDate(LocalDate date) {
        return transactionMapper.findByAutoGeneratedAndTransactionDate(date);
    }
}
